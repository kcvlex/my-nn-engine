[package]
name = "my-nn-engine-test-tools"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "my-nn-engine-test-tools"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
my-nn-engine = { path = "../.." }
tempfile = { workspace = true }
